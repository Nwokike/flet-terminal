- `[x]` Implement `FletTerminal` Python wrapper control (`packages/flet-terminal/src/flet_terminal/`)
- `[x]` Implement `flet_terminal` Dart/Flutter extension classes (`packages/flet-terminal/src/flutter/`)
- `[x]` Migrate Flet/xterm API calls inside `src/flutter/flet_terminal/lib/src/flet_terminal.dart`
  - [x] Update to `xterm: ^4.0.0` for full Flutter 3.44+ compatibility (`KeyEvent` vs `RawKeyEvent`)
  - [x] Migrate `TerminalView` arguments (`qt.TerminalView`, `FocusNode`)
  - [x] Update terminal clearing methods (`_terminal.buffer.clearScrollback()` and `_terminal.buffer.clear()`)
  - [x] Update `Control` dictionary property fetching (`widget.control.get<Map>("theme")`)
  - [x] Update `_terminalController.setSelection` to use `createAnchor(x, y)` and `SelectionMode.line`
- [x] Verify complete local build (`flet build web --wasm`) succeeds with zero errors (`task-1222`)
- [x] Commit and push all changes to `Nwokike/flet-terminal` to trigger automated multi-platform build (`build-all.yml`)
- `[x]` Rebuild `src/views/terminal_view.py` using `FletTerminal` and a native accessory keys bar
- `[x]` Validate codebase using `ruff` and compile check
